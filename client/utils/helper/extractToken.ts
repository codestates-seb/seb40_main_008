// export function extractToken(cookie: string) {
	// const indexOfCookieStart =
	// 	cookie.indexOf('accessToken') + 'accessToken='.length;
	// const indexOfCookieEnd = cookie.indexOf(';', indexOfCookieStart);
	// const token = cookie.slice(indexOfCookieStart, indexOfCookieEnd + 1);
// 	const token = cookie;

// 	return token;
// }
